{
  "campaign_template_version": "v2.experiment_template.1",
  "template_id": "parameter_sweep",
  "template_type": "parameter_sweep",
  "description": "Deterministic parameter grid over declared seeds.",
  "required_fields": ["parameter_grid", "random_seeds", "expected_outputs"],
  "deterministic": true,
  "metadata": {
    "scientific_scope": "Computational template only."
  }
}
