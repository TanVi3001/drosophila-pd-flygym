# Validation Guide

Experiment validation checks:

- schema fields;
- configuration hash;
- provenance source;
- deterministic random seeds;
- expected output declarations;
- scientific-scope guardrails;
- generated artifact integrity;
- deterministic replay through linked campaigns when outputs exist.

Validation confirms repository consistency and artifact integrity only. It does
not validate biological interpretation.
