# Fly Studio Rendering Core

The Fly Studio Rendering Core extends the Fly Studio architecture with modular rendering, animation, and plugin capabilities. It is designed to be engine independent, allowing future rendering backends like MuJoCo, OpenGL, Three.js, Blender, and Unity to implement the abstract interfaces.
